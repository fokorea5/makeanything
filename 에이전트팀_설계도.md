# MakeAnything 에이전트 팀 설계도

> 이 문서는 MakeAnything 레포지토리의 에이전트 팀 아키텍처를 설명합니다.
> 제작자(정찬)가 무언가를 만들어달라고 요청하면, 이 구조가 자동으로 가동됩니다.

---

## 1. 전체 흐름

```
제작자 요청
    │
    ▼
┌─────────────────────────────────────────────────────┐
│               오케스트레이터 (Team Lead)                │
│                                                       │
│   DISCOVER → PLAN → DO → CHECK → ACT                │
│                                                       │
│   각 단계마다 제작자에게 보고 → 승인 후 다음 단계     │
└─────────────────────────────────────────────────────┘
```

---

## 2. 단계별 상세

### DISCOVER (발견)
**목적**: 요구사항 구체화 + 기술 실현 가능성 판단

- 제작자에게 5가지를 확인:
  1. 핵심 기능 1~3개
  2. 플랫폼 (웹 / 데스크톱 / 모바일 / 스크립트)
  3. 외부 의존성 (외부 API, 서드파티 서비스)
  4. 기술 선호 (언어/프레임워크, "알아서"도 OK)
  5. 완성물 전달 방식 (소스코드 / 실행파일 / zip / 도커)
- 외부 API 의존 시 → **오라클** 소환하여 API 조사
- 구체화 완료 → `.plan.md` 초안 → PLAN 진입

### PLAN (계획)
**목적**: 실행 계획 + 비판 검증 + 팀 편성

| 순서 | 작업 | 비고 |
|------|------|------|
| 1 | 외부 API 의존 판단 | YES → 오라클 소환 |
| 2 | `.plan.md` 작성 | 사용자 원문 + AC 목록 + 전달 방식 |
| 3 | 비판 루프 (최대 2회) | 비판자 소환 → CRITICAL 있으면 수정 → 재비판 |
| 4 | 교훈 DB 검색 | 이전 프로젝트 유사 문제 확인 |
| 5 | **세포 분화** | 프로젝트 규모별 팀 편성 (아래 참조) |
| 6 | Pre-mortem | 실패 시나리오 3개 작성 |
| 7 | 제작자 승인 | 승인 후 DO 진입 |

### DO (실행)
**목적**: 설계 → 개발 → 통합

```
┌─────────────────────────────────────────┐
│  1단계: 설계 (병렬)                       │
│  ┌─────────────┐  ┌──────────────┐       │
│  │   설계자     │  │  UI디자이너   │       │
│  │ DESIGN.md   │  │ ui_design.md │       │
│  └──────┬──────┘  └──────┬───────┘       │
│         │                │               │
│  2단계: 개발 (병렬)                       │
│  ┌──────▼──────┐  ┌──────▼───────┐       │
│  │  Backend    │  │  Frontend    │       │
│  │ src/api/    │  │ src/ui/      │       │
│  │ src/db/     │  │ src/pages/   │       │
│  └─────────────┘  └──────────────┘       │
└─────────────────────────────────────────┘
```

- 설계자 완료 → 통합 계약 + 시그니처 사양 확인 → 개발자 투입
- 개발자는 DESIGN.md의 계약과 시그니처를 반드시 준수
- 임의 변경 시 FREEZE 선언 필수

### CHECK (검증)
**목적**: 통합 점검 + QA 검증

1. **오케스트레이터 통합 점검** (QA 전)
   - 통합 계약의 파일/경로/이벤트명이 실제 코드와 일치하는지
   - 백엔드-프론트 연결점 확인
2. **QA 에이전트 소환**
   - `.plan.md`의 AC를 하나씩 PASS/FAIL 판정
   - 보안 에이전트가 있으면 보안 보고서의 FAIL도 처리
3. FAIL → 개발자에게 수정 Task → 재검증

### ACT (전달)
**목적**: 패키징 + 전달 + 교훈 추출

- `.plan.md`의 전달 방식에 따라 DevOps 2단계 소환 (필요 시)
- 학습자 소환 → 교훈 추출 → `lessons.db`에 기록
- 제작자에게 최종 보고

---

## 3. 에이전트 목록

### 코어 5명 (매 프로젝트 참여)

| 에이전트 | 모델 | 역할 | 산출물 |
|----------|------|------|--------|
| **오케스트레이터** | (메인 세션) | Team Lead. 코드 작성 안 함. 흐름 제어, 조율, 보고 | 보고 양식 |
| **설계자** | opus | DESIGN.md 작성. 통합 계약 + 시그니처 사양 정의. 공유 타입 관리 (`src/shared/`) | `DESIGN.md`, `src/shared/` |
| **Backend 개발자** | sonnet | 서버, API, DB, 로직 구현 | `src/api/`, `src/db/`, `src/models/` |
| **Frontend 개발자** | sonnet | UI, 페이지, 스타일 구현 | `src/ui/`, `src/pages/`, `src/styles/` |
| **QA** | opus | `.plan.md`의 AC별 PASS/FAIL 판정. 공격자 마인드셋 | `reports/qa_report.md` |

### On-Demand 8명 (필요 시 소환)

| 에이전트 | 모델 | 소환 조건 | 역할 | 산출물 |
|----------|------|-----------|------|--------|
| **비판자** | opus | **모든 프로젝트** | `.plan.md`의 결함 탐색. CRITICAL/MINOR 분류 | `reports/critique_report.md` |
| **UI디자이너** | opus | 프론트가 있는 프로젝트 | 레이아웃, 색상, 동선, 상태별 UI 설계 | `ui_design.md` |
| **보안** | opus | auth/pay/security 포함 시 | 인증, 입력 검증, 저장 보안, 정보 노출 검사 | `reports/security_report.md` |
| **디버거** | sonnet | SOS 발생 시 | 근본 원인 특정 + 수정. 환경 문제는 보고만 | `reports/debugger_report.md` |
| **DevOps** | haiku | 패키징 필요 시 | Dockerfile, 실행 스크립트, zip 패키징 | `reports/devops_report.md` |
| **문서** | haiku | 필요 시 | README, docs/ 작성 | `reports/doc_report.md` |
| **오라클** | sonnet | 외부 API/기술 조사 필요 시 | 웹 검색으로 API, 라이브러리, 기술 스택 조사 | `reports/oracle_report.md` |
| **학습자** | haiku | 프로젝트 완료 후 | 교훈 추출 → `lessons.db` 기록 | `reports/learner_report.md` |

---

## 4. 세포 분화 (프로젝트 규모별 팀 편성)

오케스트레이터가 PLAN 단계에서 프로젝트 성격을 보고 팀을 편성합니다.

```
┌──────────────────────┬─────────────────────────────────────┬──────┐
│ 프로젝트 유형         │ 투입 에이전트                         │ 인원 │
├──────────────────────┼─────────────────────────────────────┼──────┤
│ 간단 스크립트         │ 비판자 + Backend + QA               │ 3명  │
│ 프론트만 (정적)       │ 비판자 + UI디자이너 + Frontend + QA  │ 4명  │
│ API/백엔드만          │ 비판자 + 설계자 + Backend + QA       │ 4명  │
│ 풀스택               │ 비판자 + 설계자∥UI디자이너            │ 6명  │
│                      │ + Backend∥Frontend + QA              │      │
├──────────────────────┼─────────────────────────────────────┼──────┤
│ + auth/pay/security  │ + 보안 에이전트                      │ +1   │
│ + 패키징 필요         │ + DevOps (2단계)                     │ +1   │
└──────────────────────┴─────────────────────────────────────┴──────┘

∥ = 병렬 실행
```

**비판자**는 On-Demand이지만 **모든 프로젝트에서 필수** 소환.
**UI디자이너**는 프론트가 있는 프로젝트에서 **설계자와 병렬** 소환.

---

## 5. 파일 소유권

각 에이전트는 자기 소유 파일에만 쓸 수 있습니다.

```
오케스트레이터  →  .plan.md, 보고 양식
설계자         →  DESIGN.md, src/shared/
UI디자이너     →  ui_design.md
Backend       →  src/api/, src/db/, src/models/ (+ Task에서 지정된 파일)
Frontend      →  src/ui/, src/pages/, src/styles/ (+ Task에서 지정된 파일)
QA            →  reports/qa_report.md (코드 수정 금지)
보안           →  reports/security_report.md (코드 수정 금지)
디버거         →  src/ 긴급 수정 가능 (범위 초과 시 보고)
```

남의 파일은 **읽기만** 가능합니다.

---

## 6. 통합 계약 시스템

설계자가 DESIGN.md에 작성하는 **"통합 계약"** 섹션이 핵심입니다.

### 포함 항목
- 파일 이름과 경로 (예: `pages/dashboard.html`)
- URL 라우팅 (예: `GET /api/users → 200`)
- WebSocket/이벤트 이름 (예: `__nugget_api_capture__`)
- 환경 변수 이름 (예: `API_KEY`)
- 스토리지 키 (예: `nugget_settings`)

### 규칙
```
개발자  →  계약에 명시된 이름을 그대로 사용. 임의 변경 금지.
변경 필요 시  →  오케스트레이터에게 보고 → 설계자 승인 후 변경.
QA  →  통합 계약의 모든 항목이 실제 코드와 일치하는지 검증.
오케스트레이터  →  DO 완료 후 CHECK 전에 통합 점검 수행.
```

---

## 7. 예외 처리 프로토콜

### SOS (긴급 디버깅)
```
같은 에러 3회 반복 → 개발자가 "SOS: [에러] 3회 실패" 보고
→ 오케스트레이터가 디버거 소환
→ 디버거가 근본 원인 특정 + 수정
→ QA 재검증
```

### FREEZE (설계 변경 필요)
```
코드 수정만으로 해결 불가한 문제 발견
→ "FREEZE: [이유]" 보고
→ 모든 에이전트 작업 중지
→ 설계자와 수정 방안 논의 → DESIGN.md 반영
→ 개발자에게 수정 Task
→ QA + 보안 재검증 → PASS → "작업 재개"
```

### 즉시 보고 대상
- 보안 취약점, 데이터 손실 위험
- 같은 에러 3회 재시도 후 미해결
- `.plan.md`와 현실 불일치
- FREEZE 발견

---

## 8. 보고 체계

```
각 에이전트
   │
   ├─ 작업 완료 후 1회 보고
   │   ├─ 상세: reports/{자기이름}_report.md
   │   └─ 요약: 오케스트레이터에게 전달
   │
   ▼
오케스트레이터
   │
   ├─ 각 단계마다 제작자에게 보고 양식 전달
   │
   ▼
┌─────────────────────────────────────┐
│ ▶ 현재 단계: [DISCOVER/PLAN/DO/    │
│              CHECK/ACT]             │
│ ▶ 투입 에이전트: [목록]              │
│                                     │
│ 📌 진행 내용 요약                    │
│ 🔍 의사결정 필요 사항 (있으면)        │
│ ⏳ 제작자 승인 대기 중                │
└─────────────────────────────────────┘
```

---

## 9. 모델 배치 전략

비용과 성능의 균형을 위해 역할별 모델을 다르게 배치합니다.

```
opus (최고 성능)  →  설계자, QA, 보안, 비판자, UI디자이너
                    → 정확성이 중요한 분석/검증 역할

sonnet (균형)     →  Backend, Frontend, 디버거, 오라클
                    → 코드 작성 + 조사 역할

haiku (경량)      →  DevOps, 문서, 학습자
                    → 정형화된 반복 작업
```

---

## 10. 디렉토리 구조

```
makeanything/
├── CLAUDE.md                    # 메인 에이전트 규칙 (이 설계도의 근거)
├── .plan.md / .plan.v1.1.md     # 프로젝트별 실행 계획
├── DESIGN.md                    # 설계 문서 (설계자 작성)
├── ui_design.md                 # UI 설계 문서 (UI디자이너 작성)
│
├── prompts/
│   ├── agents/                  # 에이전트별 프롬프트 (13개)
│   │   ├── orchestrator.md      # 오케스트레이터
│   │   ├── architect.md         # 설계자
│   │   ├── developer-backend.md # Backend 개발자
│   │   ├── developer-frontend.md# Frontend 개발자
│   │   ├── qa.md                # QA
│   │   ├── challenger.md        # 비판자
│   │   ├── ui-designer.md       # UI디자이너
│   │   ├── security.md          # 보안
│   │   ├── debugger.md          # 디버거
│   │   ├── devops.md            # DevOps
│   │   ├── documenter.md        # 문서
│   │   ├── oracle.md            # 오라클
│   │   └── learner.md           # 학습자
│   │
│   └── playbook/                # 단계별 플레이북 (10개)
│       ├── discover.md          # DISCOVER 단계
│       ├── plan.md              # PLAN 단계
│       ├── do.md                # DO 단계
│       ├── check.md             # CHECK 단계
│       ├── act.md               # ACT 단계
│       ├── freeze.md            # FREEZE 프로토콜
│       ├── debugger.md          # SOS 디버깅
│       ├── speculative.md       # 투기적 실행
│       ├── change_request.md    # 변경 요청
│       └── qa_guide.md          # QA 가이드
│
├── reports/                     # 에이전트 보고서
│   ├── critique_report.md
│   ├── architect_report.md
│   ├── ui_report.md
│   ├── backend_report.md
│   ├── frontend_report.md
│   ├── qa_report.md
│   └── ...
│
└── memory/
    └── lessons_db.py            # 교훈 DB (학습자 관리)
```

---

## 11. v1.1 실제 적용 사례 (Nugget Chrome Extension)

이 설계도가 실제로 적용된 사례입니다.

### 요청
> "언어 선택 설정 + 다크모드를 추가해줘" (+ API 가로채기, 원격 셀렉터)

### 세포 분화 결정
**풀스택** (6명): 비판자 + 설계자∥UI디자이너 + Backend∥Frontend + QA

### 실행 흐름
```
DISCOVER  →  Chrome MV3 API 가로채기 기술 조사 (오라클 대신 직접)
PLAN      →  .plan.v1.1.md (21개 AC) → 비판자 검토 → CRITICAL 3건 수정
DO        →  설계자∥UI디자이너 병렬 → Backend∥Frontend 병렬
CHECK     →  QA 검증 → FAIL 4건 발견 → 즉시 수정 → 전체 PASS
ACT       →  최종 커밋/푸시
```

### 투입 에이전트와 산출물
| 에이전트 | 산출물 |
|----------|--------|
| 비판자 | CRITICAL 3건 + MINOR 11건 발견 → .plan.md 개선 |
| 설계자 | DESIGN.md v1.1 섹션 (13~20), types.js, constants.js |
| UI디자이너 | ui_design.md v1.1 (다크모드 팔레트, 설정 UI) |
| Backend | interceptor.js, bridge.js, i18n.js, theme.js, background.js 수정 |
| Frontend | popup/options/onboarding HTML/JS/CSS + ko.json, en.json |
| QA (오케스트레이터) | FAIL 4건 발견 및 수정: initTheme 호출 누락, 함수명 불일치 등 |
